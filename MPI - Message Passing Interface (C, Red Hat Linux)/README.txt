MPI - Message Passing Interface:
Addition of 4 new system calls for MPI (register, send, receive and poll).
Addition of 4 matching wrapper functions for the User (mpi_api.h).
A few changes in existing files for the new system calls (entry.S, sched.h, fork.c, exit.c and Makefile).